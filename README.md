### Booking overlap policy

1. We compare bookings only when they use the same resource.
2. A booking overlaps another booking when their time periods share time.
3. A booking can start exactly when another booking ends.
4. Cancelled bookings are ignored during overlap checks.
